{
  "name": "anni-project-manager-application",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://anni.io/project-manager-application",
  "dependencies": {
    "@reach/router": "^1.2.1",
    "@sindresorhus/slugify": "^0.6.0",
    "localforage": "^1.7.3",
    "react": "^16.7.0-alpha.2",
    "react-cache": "^2.0.0-alpha.1",
    "react-dom": "^16.7.0-alpha.2",
    "react-scripts": "^2.1.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && cp build/index.html build/404.html",
    "test": "react-scripts test --env=jsdom",
    "test:coverage": "react-scripts test --env=jsdom --coverage",
    "test:build": "CI=true react-scripts test --env=jsdom",
    "test:staged": "CI=true react-scripts test --env=jsdom --findRelatedTests",
    "eject": "react-scripts eject",
    "precommit": "lint-staged",
    "predeploy": "npm run test:build && npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "eslint-config-prettier": "^3.3.0",
    "eslint-plugin-prettier": "^3.0.0",
    "gh-pages": "^2.0.1",
    "husky": "^1.2.1",
    "jest-dom": "^3.0.0",
    "lint-staged": "^8.1.0",
    "lodash": "^4.17.11",
    "prettier": "^1.15.3",
    "react-sortable-hoc": "^0.8.4",
    "react-testing-library": "^5.3.2",
    "reselect": "^4.0.0"
  },
  "lint-staged": {
    "src/**/*.{js,jsx,json}": [
      "npm run test:staged",
      "eslint --fix",
      "git add"
    ]
  },
  "jest": {
    "collectCoverageFrom": [
      "src/**/*.{js,jsx}",
      "!<rootDir>/node_modules/",
      "!src/serviceWorker.js",
      "!src/index.js"
    ],
    "coverageThreshold": {
      "global": {
        "branches": 90,
        "functions": 90,
        "lines": 90,
        "statements": -10
      }
    }
  }
}
